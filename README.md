# ft_printf-
Reimplementation of the C printf function, developed at 42 Lisboa. Handles formatted output using variadic functions, supporting key specifiers like %d, %s, %x, and more. Built from scratch with clean, efficient code and strict standard function constraints.
