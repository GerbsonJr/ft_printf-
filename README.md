This project is part of the 42 Lisboa curriculum. It consists of recreating the standard C printf function from scratch, handling formatted output with precision and efficiency. The goal was to deepen understanding of variadic functions, memory management, and low-level string manipulation.

✅ Features
Supports format specifiers: %c, %s, %p, %d, %i, %u, %x, %X, and %%

Handles flags, width, and precision

Built using only allowed standard functions

Robust error handling and edge case coverage

Fully tested with custom test cases

🛠️ Technologies
Language: C

Tools: Makefile, GCC

Environment: Unix/Linux

This project was a key milestone in mastering function logic and output formatting at the system level.
